<%= form_for(@agence) do |f| %>
  <% if @agence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agence.errors.count, "error") %> prohibited this agence from being saved:</h2>

      <ul>
      <% @agence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :principalgroupe_id %><br>
    <%= f.text_field :principalgroupe_id %>
  </div>
  <div class="field">
    <%= f.label :sousgroupe_id %><br>
    <%= f.text_field :sousgroupe_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
